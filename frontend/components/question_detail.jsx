var React = require('react');
var PropTypes = React.PropTypes;

var QuestionDetail = React.createClass({

  getInitialState: function() {
    // need to know question index (from props)
    // question text
    // true answer text
    // false answer text
    
  },

  render: function() {
    return (
      <div>
        Question Detail
      </div>
    );
  }

});

module.exports = QuestionDetail;
